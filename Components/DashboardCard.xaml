<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MVPStudio_Creative_Agency.Components.DashboardCard">
 <VerticalStackLayout Margin="0">
        <Frame
            CornerRadius="15"
            BackgroundColor="{DynamicResource PurpleLighter}"
            BorderColor="Transparent"
        >
            <VerticalStackLayout HorizontalOptions="Start">

                <Label 
                    Text="{Binding CardTitle}"
                    FontFamily="Montserrat-Bold"
                    TextColor="{DynamicResource Black}"
                    FontSize="32"
                    VerticalOptions="Start" 
                    HorizontalOptions="Start"
                />

                <HorizontalStackLayout Margin="0,20,0,20">

                    <!--<Border
                        HeightRequest="80" 
                        WidthRequest="80"
                        StrokeShape="RoundRectangle 40,40,40,40"
                        HorizontalOptions="Center"
                        StrokeThickness="3"
                        Margin="0,0,0,0"
                    >

                        <Image
                            Aspect="AspectFill"
                            Source="person.png"
                            SemanticProperties.Description="Cute dot net bot waving hi to you!"
                            HeightRequest="80"
                            WidthRequest="80"
                            VerticalOptions="Center"
                            HorizontalOptions="Center"
                        />

                    </Border>-->

                    <Image Source="person.jpeg" WidthRequest="80" HeightRequest="80" Aspect="AspectFill" Margin="0">
                        <Image.Clip>
                            <EllipseGeometry Center="40,40" RadiusX="40" RadiusY="40" />
                        </Image.Clip>
                    </Image>

                    <Image Source="person2.jpeg" WidthRequest="80" HeightRequest="80" Aspect="AspectFill" Margin="-30,0,0,0">
                        <Image.Clip>
                            <EllipseGeometry Center="40,40" RadiusX="40" RadiusY="40" />
                        </Image.Clip>
                    </Image>

                    <Image Source="person3.jpeg" WidthRequest="80" HeightRequest="80" Aspect="AspectFill" Margin="-30,0,0,0">
                        <Image.Clip>
                            <EllipseGeometry Center="40,40" RadiusX="40" RadiusY="40" />
                        </Image.Clip>
                    </Image>

                </HorizontalStackLayout>

                <Label FontFamily="Hind-Light" FontSize="16" TextColor="{DynamicResource Purple}">

                    <Label.FormattedText>
                        <FormattedString>
                            <Span Text="List of "/>
                            <Span Text="{Binding CardTitle}" />
                        </FormattedString>
                    </Label.FormattedText>

                </Label>
  
            </VerticalStackLayout>

        </Frame>
    </VerticalStackLayout>
</ContentView>
